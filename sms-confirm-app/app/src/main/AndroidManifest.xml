<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- فقط برای خواندن پیامک‌های ورودی؛ اپ هیچ پیامکی نمی‌فرستد و به مخاطبین/تماس‌ها دسترسی ندارد -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- برای اینکه اندروید اپ را در Doze/battery-optimization نکشد و پیامک از دست نرود -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@android:drawable/ic_dialog_email"
        android:theme="@style/Theme.SmsConfirm"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- گیرنده‌ی پیامک: فقط پیامک‌های SMS_RECEIVED را می‌گیرد؛ فیلتر واقعیِ
             «فقط از طرف بانک پاسارگاد» داخل کد Kotlin انجام می‌شود، نه اینجا،
             چون شناسه‌ی فرستنده در XML قابل تنظیم نیست. -->
        <receiver
            android:name=".SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
